cmake_minimum_required(VERSION 2.8)
project( colorTrack )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( colorTrack colorTrack.cpp )
target_link_libraries( colorTrack ${OpenCV_LIBS} )
